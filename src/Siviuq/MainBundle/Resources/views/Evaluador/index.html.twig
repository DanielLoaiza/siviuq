{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Lista de evaluadores</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Correo</th>
                <th>Area</th>
                <th>Acción</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.correo }}</td>
                <td>{{ entity.area }}</td>
                <td>
                    <a href="{{ path('evaluador_assign', {'evaluadorId': entity.id,'proyectoId':app.request.get('id') }) }}">
                    <img src="{{
     					asset('img/assigncheck.png') }}" title="Asignar este evaluador"</img></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('evaluador_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
